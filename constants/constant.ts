export enum ApiAction {
  APPLY_LEAVE,
  CREATE_EMPLOYEE,
  GET_LEAVE_STATUS,
  ACCEPT_REJECT_LEAVE,
  ACCEPT_LEAVE,
  REJECT_LEAVE,
  CREATE_DEPARTMENT,
  GET_USER,
}

export enum LeaveStatusType {
  PENDING,
  ACCEPTED,
  REJECTED,
}
