import React from 'react'
import firebase from '../firebase/clientApp'
import { getSession } from 'next-auth/client'

export default function LeaveStatus() {
  return <div>Pending Leave Status</div>
}
